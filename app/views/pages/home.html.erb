<% content_for(:title) { "Vera Trade — Le magazine des voitures qui ont une âme" } %>
<% content_for(:description) { "Marketplace française des youngtimers, voitures de collection et préparations. Chaque annonce, un article éditorial. Provenance documentée, échange véhicule + soulte, escrow." } %>

<%# ============================================================================
    HOMEPAGE — Cinematic Archivist v2 (racing green)
    Refonte 2026-04 : structure éditoriale BaT-style, 8 chapitres
    ============================================================================ %>

<%= render "pages/home/hero" %>
<%= render "pages/home/search_bar" %>
<%= render "pages/home/trust_bar" %>

<%= render "pages/home/pick_of_week" if @featured_listing %>

<%= render "pages/home/segments" %>

<%= render "pages/home/editor_choice" if @curated_listings.any? %>

<%= render "pages/home/auctions_strip" %>

<%= render "pages/home/exchange_pitch" %>

<%= render "pages/home/quote" %>

<%= render "pages/home/sell_cta" %>
